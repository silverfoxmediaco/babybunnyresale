{
  "name": "babybunnyresale",
  "version": "1.0.0",
  "description": "Baby Bunny Resale - Full Stack Application",
  "scripts": {
  "build": "cd frontend && npm install && npx vite build",
  "start": "cd backend && npm install && node server.js"

},

  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}